# Default game-start hook — assumes "normal" mode.
# If you have multiple modes, call the matching ranked:on_start_<mode>
# function from the right place in the bingo pack instead.
rankedreport start normal
